import { inicializarApp } from "./components/app.js";
import { mostrarLogin } from "./login/login.js";

document.addEventListener("DOMContentLoaded", () => {
    // Iniciar mostrando el login
    mostrarLogin();
});
